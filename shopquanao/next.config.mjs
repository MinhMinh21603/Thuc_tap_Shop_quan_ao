/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.pinimg.com'], // Thêm domain của ảnh
    },
};

export default nextConfig; // Chỉ cần một export default